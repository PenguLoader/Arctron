{
  "name": "arctron-vite-example",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:main": "esbuild src/main-process.ts --bundle --platform=neutral --format=esm --outfile=dist/main-process.js --watch",
    "build": "vite build",
    "build:main": "esbuild src/main-process.ts --bundle --platform=neutral --format=esm --outfile=dist/main-process.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "arctron": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.6.0",
    "vite": "^5.0.0"
  }
}
